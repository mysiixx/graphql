# PostgreSQL DB for tests
spring.datasource.url=jdbc:postgresql://localhost:5432/matchme_test
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=8ded6076

# JWT keys path
jwt.public.key.path=file:../JWT-keys/public.key
jwt.private.key.path=file:../JWT-keys/private.key

spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.sql.init.mode=always
app.upload-dir=target/test-uploads/avatars
app.upload-url-prefix=/uploads/avatars
